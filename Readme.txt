This application helps to detect the number plates in realtime and crop them out in a different window for better insights using the Haar cascade training datasets.
Instructions:

Required libraries:

    sudo apt install python3.9
    pip install opencv-python==4.5.4.60
    pip3 install numpy
    
